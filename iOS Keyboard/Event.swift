//
//  Event.swift
//  iOS Keyboard
//
//  Created by George Birch on 9/7/23.
//

import Foundation
import OSLog
/*
struct Event {
    
    let id: String
    let title: String
    let startTime: Date?
    let endTime: Date?
    let timezone: TimeZone?
    let location: String?
    let locationUrl: URL?
    let addToGCalUrl: URL
    let eventUrl: URL
    let description: String?
    let timeLines: [String]
    //TODO: figure out how to store image
//    let image: ÷
    
    let logger: Logger!
    
    init(from eventData: EventData) {
        logger = Logger(subsystem: "com.withdunbar.iOS-Keyboard", category: "Event with id: \(id)")
        do {
            id = eventData.id
            title = eventData.title
            startTime = try generateDate(date: eventData.startDate, time: eventData.startTime)
        } catch {
            logger.notice("Unable to parse event data: \(error)")
        }
    }
    
    private func generateDate(date: String, time: String?) throws -> Date? {
        //TODO: fill
        return nil
    }
    
}
*/
/*
 
 struct EventData: Decodable {
     let id: String
     let title: String
     let startTime: String?
     let endTime: String?
     let startDate: String
     let endDate: String
     let timezone: String?
     let location: String?
     let locationUrl: String?
     let addToGcalUrl: String
     let eventUrl: String
     let description: String?
     let timeLines: [String]
     let image: ImageData?
 }
 */
